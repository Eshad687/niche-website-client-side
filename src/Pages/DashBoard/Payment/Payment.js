import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';

const Payment = () => {
    return (
        <Box>
            <Typography variant="h4" sx={{ color: 'rgb(219, 75, 50)', textAlign: 'center' }}>Payment System Coming Soon!</Typography>
        </Box>
    );
};

export default Payment;